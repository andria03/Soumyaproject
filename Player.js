class Player
{
    constructor(){
        this.name=null;
        this.cards=[];
    }

    servecards(){
        var rand= Math.round(random(0,1));
        var rand2= Math.round(random(0,1));

        
    }
}